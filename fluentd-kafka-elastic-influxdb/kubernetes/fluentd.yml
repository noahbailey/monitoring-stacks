---
apiVersion: v1
kind: Service
metadata:
  name: fluentd
  labels:
   app: fluentd
spec:
  ports:
  - name: forward
    protocol: TCP
    port: 5000
  - name: forward-udp
    protocol: UDP
    port: 5000
  selector:
    app: fluentd
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fluentd
  labels:
    app: fluentd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fluentd
  template:
    metadata:
      labels:
        app: fluentd
    spec:
      containers:
      - name: fluentd
        image: noahbailey/fluentd-output:latest
        ports:
        - name: forward
          containerPort: 5000
          protocol: TCP
        - name: forward-udp
          containerPort: 5000
          protocol: UDP
        livenessProbe:
          tcpSocket:
            port: forward
          initialDelaySeconds: 20
          periodSeconds: 10
        env:
        - name: KAFKA_BROKER
          value: "kafka-0.kafka-service:9092,kafka-1.kafka-service:9092,kafka-2.kafka-service:9092"
        - name: ELASTICSEARCH_HOST
          value: "elasticsearch.default.svc.cluster.local"
        - name: INFLUXDB_HOST
          value: "http://influxdb.default.svc.cluster.local:8086"
        - name: INFLUXDB_TOKEN
          value: "uflApic2-zr9xakwXpeWfn8sKEzxqspc4vnRE3BAIIoXXvrMRN9DLEKKIZLNJ-JwbkgXQidmPvukQ32mb4uMvQ=="
---
apiVersion: v1
kind: Service
metadata:
  name: fluentd-input
  labels:
   app: fluentd-input
spec:
  type: NodePort
  ports:
  - name: forward
    protocol: TCP
    port: 5000
    nodePort: 32100
  - name: forward-udp
    protocol: UDP
    port: 5000
    nodePort: 32100
  selector:
    app: fluentd-input
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fluentd-input
  labels:
    app: fluentd-input
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fluentd-input
  template:
    metadata:
      labels:
        app: fluentd-input
    spec:
      containers:
      - name: fluentd-input
        image: noahbailey/fluentd-input:latest
        ports:
        - name: forward
          containerPort: 5000
          protocol: TCP
        - name: forward-udp
          containerPort: 5000
          protocol: UDP
        livenessProbe:
          tcpSocket:
            port: forward
          initialDelaySeconds: 20
          periodSeconds: 10
        env:
        - name: KAFKA_BROKER
          value: "kafka-0.kafka-service:9092,kafka-1.kafka-service:9092,kafka-2.kafka-service:9092"
